% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_series.R
\name{adjust_for_split}
\alias{adjust_for_split}
\title{Adjust a time series for a stock split}
\usage{
adjust_for_split(data, split_date, split_ratio, value_col, date_col = "date")
}
\arguments{
\item{data}{A data frame.}

\item{split_date}{A single date coercible via \code{\link[lubridate:as_date]{lubridate::as_date()}}.}

\item{split_ratio}{A positive numeric scalar giving the split ratio.}

\item{value_col}{String. Name of the numeric column to adjust.}

\item{date_col}{String. Name of the date column in \code{data}.}
}
\value{
A data frame with the same columns as \code{data}, where \code{value_col} has been
adjusted for rows with dates strictly before \code{split_date}.
}
\description{
Adjusts values in a numeric column for observations strictly before a given
split date by dividing them by the supplied split ratio.
}
\details{
The function parses \code{split_date} and \code{data[[date_col]]} with
\code{\link[lubridate:as_date]{lubridate::as_date()}}. Rows with missing dates are left unchanged. Rows with
unparseable non-missing dates trigger an error.

For rows where the parsed date is strictly earlier than \code{split_date}, the
values in \code{value_col} are divided by \code{split_ratio}.
}
\examples{
df <- data.frame(
  date = c("2024-01-01", "2024-01-02", "2024-01-03"),
  price = c(300, 330, 120)
)

adjust_for_split(
  data = df,
  split_date = "2024-01-03",
  split_ratio = 3,
  value_col = "price"
)

}
\seealso{
Other fund/index workflow functions: 
\code{\link{add_data_loader}()},
\code{\link{build_all_series}()},
\code{\link{clear_data_loaders}()},
\code{\link{clear_storage}()},
\code{\link{get_storage}()},
\code{\link{import_fund}()},
\code{\link{join_env}()},
\code{\link{run_data_loaders}()},
\code{\link{store_timeseries}()}
}
\concept{fund/index workflow functions}
