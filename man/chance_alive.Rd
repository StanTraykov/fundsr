% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{chance_alive}
\alias{chance_alive}
\title{Compute conditional survival (chance alive) by age}
\usage{
chance_alive(lt, pop_name, age0)
}
\arguments{
\item{lt}{A life table tibble as returned by \code{\link[=read_life_table]{read_life_table()}}, containing
at least \code{PopName}, \code{Year}, \code{Age}, and \code{lx}.}

\item{pop_name}{Population code (HMD \code{PopName}) to filter within \code{lt}
(e.g. \code{"BGR"}, \code{"USA"}, \code{"DEUTNP"}).}

\item{age0}{Baseline age (integer). Returned ages start at \code{age0}.}
}
\value{
A tibble with columns \code{Year}, \code{Age}, and \code{chance_alive}, sorted by
\code{Year} then \code{Age}.
}
\description{
Computes the conditional probability of being alive at each age \code{x >= age0},
given survival to \code{age0}, from an HMD-style period life table. For each year,
the returned series is:
\code{chance_alive(x | age0) = lx(x) / lx(age0)}.
}
\examples{
\dontrun{
lt_m <- read_life_table(file.path("data", "life"), sex = "m", look_back = 10)
ca <- chance_alive(lt_m, pop_name = "BGR", age0 = 27)
ca
}
}
