% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{chance_alive_es_aasmr}
\alias{chance_alive_es_aasmr}
\title{Compute cohort-style survival from Eurostat EUROPOP2023 mortality assumptions}
\usage{
chance_alive_es_aasmr(es, geo, sex, age0, start_year = NULL)
}
\arguments{
\item{es}{A tibble as returned by \code{\link[=read_es_aasmr]{read_es_aasmr()}}.}

\item{geo}{Eurostat geo code (e.g. \code{"BG"}, \code{"NL"}).}

\item{sex}{Sex code: \code{"m"} or \code{"f"} (case-insensitive).}

\item{age0}{Baseline age (integer).}

\item{start_year}{Starting calendar year (integer). If \code{NULL}, uses the
earliest year available for the selected \code{geo/sex}.}
}
\value{
A tibble with columns \code{geo}, \code{sex}, \code{projection}, \code{Year}, \code{Age}, \code{mx},
\code{qx}, \code{chance_alive}.
}
\description{
Computes conditional survival (chance alive) for a person aged \code{age0} in
\code{start_year} using Eurostat EUROPOP2023 age-specific mortality rate assumptions
(dataset \code{proj_23naasmr}). The computation follows a cohort path (diagonal):
for age \code{age0 + k} it uses the mortality rate for year \code{start_year + k}.
}
\details{
The result includes two projection variants: baseline (\code{BSL}) and lower
mortality (\code{LMRT}).
}
\examples{
\dontrun{
es <- read_es_aasmr(file.path("data", "life"))
ca <- chance_alive_es_aasmr(es, geo = "NL", sex = "m", age0 = 42, start_year = 2022)
p <- plot_chance_alive_es_aasmr(ca, sex = "m", population = "NL")
p
}
}
\seealso{
Other survival curve functions: 
\code{\link{chance_alive}()},
\code{\link{plot_chance_alive}()},
\code{\link{plot_chance_alive_es_aasmr}()},
\code{\link{read_es_aasmr}()},
\code{\link{read_life_table}()}
}
\concept{survival curve functions}
