% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fund_imp.R
\name{coalesce_join_suffixes}
\alias{coalesce_join_suffixes}
\title{Coalesce suffixed join columns into unsuffixed base columns}
\usage{
coalesce_join_suffixes(df, suffixes = c(".x", ".y"))
}
\arguments{
\item{df}{A data frame or tibble produced by joins.}

\item{suffixes}{Character vector of length 2 giving the suffixes to
coalesce, in priority order. For example, \code{c(".x", ".y")} uses
\code{.x} as the primary source and falls back to \code{.y} when \code{.x} is \code{NA}.}
}
\value{
A tibble with the same rows as \code{df}, where any pairs of
suffixed columns (e.g. \code{name.x} / \code{name.y}) are replaced by a single
unsuffixed column (e.g. \code{name}) containing the coalesced values.
}
\description{
Helper for post-processing join results that use suffixes such as
\code{.x} / \code{.y}. For each base name that appears with both suffixes
(e.g. \code{FTAW.x} and \code{FTAW.y}), this function creates a new column
\code{FTAW} as \code{dplyr::coalesce(FTAW.x, FTAW.y)} and drops the suffixed
columns. The order of \code{suffix} controls which column is preferred.
}
\keyword{internal}
