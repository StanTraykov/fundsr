% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{fundsr_options}
\alias{fundsr_options}
\title{Set fundsr package options}
\usage{
fundsr_options(
  data_dir = NULL,
  out_dir = NULL,
  px_width = NULL,
  internal_png = NULL,
  export_svg = NULL,
  ticker_map = NULL,
  inkscape = NULL,
  reload = NULL,
  fund_urls = NULL,
  verbosity = NULL
)
}
\arguments{
\item{data_dir}{Directory containing fund data files (sets \code{fundsr.data_dir}). Note: fundsr may
try to write to this directory (see \code{download_fund_data()}).}

\item{out_dir}{Output directory for plots/exports (sets \code{fundsr.out_dir}).}

\item{px_width}{Default PNG export width in pixels (sets \code{fundsr.px_width}).}

\item{internal_png}{Logical; whether to save an internal PNG immediately when
exporting plots (sets \code{fundsr.internal_png}).}

\item{export_svg}{Logical; whether to save SVGs and queue Inkscape exports
(sets \code{fundsr.export_svg}).}

\item{ticker_map}{Named character vector mapping "primary" tickers (series
table column names) to translated tickers (sets \code{fundsr.ticker_map}).}

\item{inkscape}{Inkscape executable (path or command name) used by export
helpers (sets \code{fundsr.inkscape}).}

\item{reload}{Logical; default value for forcing re-import of cached
objects (sets \code{fundsr.reload}).}

\item{fund_urls}{Named character vector or named list of URLs for fund data
downloads (sets \code{fundsr.fund_urls}).}

\item{verbosity}{Integer verbosity level (sets \code{fundsr.verbosity}). Use 0 to silence
informational messages, 2 to emit more detailed progress/diagnostic messages, and
4 to force all message types even when they are disabled via function arguments
(e.g. \code{messages} for \code{roll_diffs()}).}
}
\value{
Invisibly returns a named list of the previous values of the options
that were changed (as returned by \code{options()}).
}
\description{
Convenience wrapper around \code{options()} for setting common \verb{fundsr.*} options.
}
\details{
All arguments default to \code{NULL}. If an argument is left as \code{NULL}, the
corresponding \verb{fundsr.*} option is left unchanged.
}
\examples{
fundsr_options(verbosity = 4)
fundsr_options(
    data_dir = file.path("data", "funds"),
    out_dir = "output",
    px_width = 1300,
    ticker_map = c(
        ticker1 = "ticker2",
        ticker3 = "ticker4"
    )
)
}
\seealso{
\code{\link[=add_fund_urls]{add_fund_urls()}} to add/update entries in \code{fundsr.fund_urls}.

Other config functions: 
\code{\link{fundsr_default_session}()},
\code{\link{fundsr_session}()},
\code{\link{reset_state}()}
}
\concept{config functions}
