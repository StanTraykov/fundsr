% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fund_imp.R
\name{load_all_series}
\alias{load_all_series}
\title{Load and join all series from registered data loaders}
\usage{
load_all_series(by = "date", ...)
}
\arguments{
\item{by}{Column name to join by and to sort by. Defaults to \code{"date"}.}

\item{...}{Additional arguments forwarded to \code{\link[=join_env]{join_env()}} (e.g. \verb{late =},
\verb{coalesce_suffixed =}, etc.).}
}
\value{
A tibble containing all joined series, sorted by \code{by}.
}
\description{
Runs \code{\link[=run_data_loaders]{run_data_loaders()}}, joins the resulting environment into a single data
frame via \code{\link[=join_env]{join_env()}}, and sorts the result by \code{by}.
}
\details{
This function is a convenience wrapper for the common workflow used in
examples and vignettes.
}
\examples{
\dontrun{

s1 <- load_all_series()
download_fund_data(redownload = TRUE)
s2 <- load_all_series(by = "date", late = "ftaw", coalesce_suffixed = c(".y", ".x")) \%>\%
  filter(date >= as_date("2013-01-01"))
}
}
