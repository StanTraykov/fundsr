% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlm.R
\name{plot_xlms}
\alias{plot_xlms}
\title{Plot Xetra Liquidity Measure (XLM) time series}
\usage{
plot_xlms(xlm_data, tickers, rgx = FALSE, gg_params = NULL, back_trans = FALSE)
}
\arguments{
\item{xlm_data}{A data frame containing XLM observations, including
columns \code{date}, \code{xlm}, \code{ticker}, and \code{name}.}

\item{tickers}{Character vector of tickers to plot, or a pattern if
\code{rgx = TRUE}.}

\item{rgx}{Logical; if \code{TRUE}, \code{tickers} is treated as a regular
expression and matched against the \code{name} column; otherwise it is
matched against the \code{ticker} column.}

\item{gg_params}{Optional list of additional ggplot components (such as
themes, scales, or labels) to be added to the base plot.}

\item{back_trans}{Logical; if \code{TRUE}, replaces values in the \code{ticker}
column \emph{after filtering} using the inverse of the mapping stored in the
\code{fundsr.xetra_map} option. This affects only what is displayed (legend
labels and color grouping), not which rows are selected.}
}
\value{
A ggplot object showing the XLM time-series curves.
}
\description{
Generates a time-series plot of XLM values for one or more ETFs, with
optional back-translation of the displayed ticker labels via the
\code{fundsr.xetra_map} option.
}
\details{
If \code{rgx = FALSE}, the data are filtered by matching \code{tickers} against the
\code{ticker} column. If \code{rgx = TRUE}, \code{tickers} is treated as a regular
expression and matched against the \code{name} column.

When \code{back_trans = TRUE}, the function reads \code{getOption("fundsr.xetra_map")},
expected to be a named character vector mapping internal identifiers to
Xetra tickers, for example:
\code{c(fwra = "fwia", acwi = "lyy0")}.
The mapping is inverted and applied to the already-filtered data so that
occurrences of Xetra tickers in the plotted data are replaced by their
internal identifiers in the plot's legend and grouping.
}
\examples{
\dontrun{
options(fundsr.xetra_map = c(fwra = "fwia", acwi = "lyy0", iwda = "eunl", cw8 = "amew"))

# Filter by tickers as they appear in xlm_data$ticker, but display back-translated labels
plot_xlms(xlm_data, tickers = c("fwia", "eunl"), back_trans = TRUE)

# Regex match against the `name` column
# (back-translation, if enabled, still happens after filtering)
plot_xlms(xlm_data, tickers = "MSCI World", rgx = TRUE, back_trans = TRUE)
}
}
