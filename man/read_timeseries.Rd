% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fund_imp.R
\name{read_timeseries}
\alias{read_timeseries}
\title{Read a time series file (CSV/TSV) with a date + one or more value columns}
\usage{
read_timeseries(file, time_unit = c("ms", "s", "us", "ns"))
}
\arguments{
\item{file}{Filename to read (relative to \code{getOption("fundsr.data_dir")}).}

\item{time_unit}{Character scalar giving the unit of the numeric \code{date} column
(Unix epoch). One of \code{"ms"} (default), \code{"s"}, \code{"us"}, \code{"ns"}.}
}
\value{
A tibble with parsed \code{date} and numeric value columns.
}
\description{
Loads a delimited file from the directory specified by \code{fundsr.data_dir},
converts the \code{date} column to a proper Date, and coerces all other columns to
numeric.
}
\details{
The reader is chosen by file extension: \code{.csv} uses \code{\link[readr:read_delim]{readr::read_csv()}} and
\code{.tsv}/\code{.tab}/\code{.txt} uses \code{\link[readr:read_delim]{readr::read_tsv()}}. Gzipped variants such as
\code{.csv.gz} and \code{.tsv.gz} are also supported.

The function assumes a column named \code{date} exists and represents a Unix
timestamp. All non-\code{date} columns are coerced with \code{as.numeric()} (non-parsable
values become \code{NA}).
}
