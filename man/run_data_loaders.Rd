% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fund_imp.R
\name{run_data_loaders}
\alias{run_data_loaders}
\title{Run registered data loaders}
\usage{
run_data_loaders(reload = FALSE)
}
\arguments{
\item{reload}{Logical scalar. If \code{TRUE}, forces a full reload by setting
\code{options(fundsr.reload = TRUE)} for the duration of this call.}
}
\value{
Invisibly returns \code{.fundsr_storage} after running the data loaders.
}
\description{
Runs the data loader registry (\code{.fundsr$data_loaders}) to populate (or refresh)
the package's storage environment (\code{.fundsr_storage}).
}
\details{
The function temporarily sets the \code{fundsr.reload} option so that data loaders
can decide whether to recompute cached objects.

The previous value of \code{getOption("fundsr.reload")} is restored on exit,
even if a data loader errors.

Data loaders are taken from \code{.fundsr$data_loaders} and are called
sequentially in registration order. Each registered function must take
no arguments.
}
