% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fund_imp.R
\name{setg}
\alias{setg}
\title{Set or update a cached object in the package storage environment}
\usage{
setg(var_name, expr, add_fi_pairs = NULL)
}
\arguments{
\item{var_name}{Name of the variable to store in \code{.fundsr_storage}.}

\item{expr}{An expression to evaluate when (re)computing the value.}

\item{add_fi_pairs}{Optional named vector or list of fund–index
pairs to append to \code{.fundsr$fund_index}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effects.
}
\description{
Evaluates an expression and assigns its result into the internal
\code{.fundsr_storage} environment under the given variable name.
The value is recomputed only if it is missing or if the global option
\code{fundsr.redo_import} is set to \code{TRUE}. Optionally updates the global
fund-index mapping.
}
\details{
The expression \code{expr} is evaluated in the caller's environment and
then stored in \code{.fundsr_storage}. This function provides memoisation-
like behaviour for costly imports or transformations that should
persist across function calls.
}
